# Contributing

## Setup

```bash
npm install
npm run check
```

## Before opening a PR

- keep `SKILL.md` metadata on a single line;
- update examples if the schema changes;
- run `npm run check` locally;
- avoid committing secrets or local `.env` files.
